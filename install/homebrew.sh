#!/bin/bash
brew update
brew install git git-lfs wget curl
brew install zsh the_silver_searcher fzf tmux direnv
# brew install rbenv && rbenv init
brew install pyenv
brew install \
    docker \
    ansible \
    coreutils \
    gh \
    postgresql@14 \
    mysql \
    sqlite

brew install --cask \
    visual-studio-code \
    around \
    google-chat \
    figma \
    tunnelblick \
    krisp \
    MonitorControl

brew install --cask \
    google-chrome \
    firefox \
    telegram \
    spotify

brew install --cask 1password 1password-cli \
    lastpass lastpass-cli \
    outline-manager \
    mullvadvpn

brew install licecap ffmpeg gifsicle

brew install browserstacklocal ngrok

brew install imageoptim unrar
#	graphviz 
# 	aria2

# brew tap codeclimate/formulae
# brew install codeclimate

# brew install mycli golang vagrant composer
# brew install overmind hivemind

# brew install --cask maccy

# brew install openjdk \
# 	gradle
# 	kotlin

